# Core Dependencies
mistralai>=1.0.0
python-dotenv>=1.0.0
rich>=13.0.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# Database & ORM
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# HTTP Client (often used alongside requests, but good to be explicit if used)
requests>=2.31.0

# Feed Parsing (mentioned for RSS/Atom feeds)
feedparser>=6.0.0

# Language Detection (mentioned as potentially useful)
langdetect>=1.0.9

# Password Hashing (mentioned for user auth)
passlib[bcrypt]>=1.7.4

# JWT (mentioned for user auth)
python-jose[cryptography]>=3.3.0

# Telegram Bot (mentioned for Telegram integration)
python-telegram-bot>=20.0

# PostgreSQL adapter for non-async use if needed (optional, but common)
psycopg2-binary>=2.9.0

# For handling dates and times (useful for timezone-aware operations)
python-dateutil>=2.8.2